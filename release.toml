disable-push=true
disable-publish=true
no-dev-version=true
consolidate-commits=true
pre-release-commit-message="deku and deku-derive release"
tag-prefix = "{{crate_name}}"
tag-message = "{{prefix}}-v{{version}}"
tag-name = "{{prefix}}-v{{version}}"
